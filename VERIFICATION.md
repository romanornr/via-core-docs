# Verification Ledger

This repository previously contained auto-generated content with fabricated code (see commit `fdceab6`). This ledger tracks which documents have since been verified line-by-line against the via-core source, and against which commit. Files not listed here have **not** been verified and may contain stale or invented claims.

Method: every concrete claim (paths, struct fields, function signatures, constants, config keys, SQL) is checked against the source tree; fabricated or stale code is replaced with verbatim current source. Docs keep full code depth by design.

## Verified against via-core `e383a2990` (2026-07-02)

| Document | Outcome |
|----------|---------|
| `inscription_interaction.md` | Corrected type catalog (7 inscribable / 12 parsed variants), bootstrap input fields, wire tags, `VIA_WI` withdrawal OP_RETURN layout, DA-flag wiring |
| `tapscript_usage.md` | Corrected taproot tweak (`merkle_root` param), `get_tr_sighashes` per-input API, type catalog |
| `utxo_management.md` | Rewritten from source: real `UtxoManager`, largest-first selection, pending-tx context, inscriber-side FIFO chaining. Fabricated selector/consolidator/fee-estimator removed |
| `musig2_implementation.md` | Updated to `TransactionBuilderConfig` API, per-input signing (`signer_per_utxo_input`), DAL-driven withdrawal sessions, derived n-of-n `required_signers` |
| `verifier_network.md` | Updated to `ViaNodeRole`, current node-builder layers, DB-persisted watcher progress, per-input signing state |
| `verifier.md` | Fabricated config/sync/resource/metrics/CLI sections replaced with real `ViaVerifierConfig`, DAL-backed gating, `BtcClientLayer`, vise metrics, versioned `via_verification` modules; depth pass added verbatim session hooks, canonical-chain DAL bodies, governance processors, real `main.rs` (bin is `verifier_server`), rebuilt storage-init section; invented `zk_agreement_threshold`/`process_system_contract_upgrade` removed |
| `withdrawal_finalization.md` | Updated to current pipeline: `via_withdrawal_dal` sessions, versioned `VIA_WI` OP_RETURN with withdrawal ids, per-input aggregation, idempotent `WithdrawalProcessor` reconciliation |
| `celestia_integration.md` | Added DaBackend selection, fallback client, `ViaDaBlob` chunking/manifest, `index` column; corrected `ViaCelestiaConfig` (auth via secrets) and `CelestiaClient` construction; depth pass added verbatim client bodies, real `via_data_availability` DDL + chunking migration, dispatcher metrics, and the real `retry` backoff policy |
| `batch_proofs.md` | Largely inherited zksync prover pipeline, verified sound; corrected witness-generator paths to `src/rounds/` |
| `zkproof_handling.md` | Corrected `via_verification` layout to per-version modules (`version_27`/`version_28`); finalization threshold made precise (`BATCH_FINALIZATION_THRESHOLD = 0.66`, not simple majority) |
| `fee_mechanism.md` | Added the real `via_fee_model` (inscription-cost-derived gas price); replaced invented fee-strategy TOML/CLI/mempool machinery with real `ViaBtcClientConfig`, `get_fee_rate` flow, hardcoded `FeeRateLimits`; depth pass added verbatim `FeeStrategy`/`WithdrawalFeeStrategy::apply_fee_to_outputs`, `_verify_withdrawals`, full `get_fee_rate` body; invented `estimate_fee_sats` and `user_should_receive` claims removed |
| `l1_watcher.md` | Current `BitcoinClient`/`BitcoinInscriptionIndexer`/`VerifierBtcWatch` shapes (SystemWallets, DB cursor, reorg gate); idempotent `WithdrawalProcessor`; full message catalog; threshold as shared constant; depth pass added verbatim `ViaL1Deposit`/`ViaPriorityOpId`, governance processor, full `loop_iteration`, real `ViaBtcWatchConfig` (`block_confirmations`, not `confirmations_for_btc_msg`) and real metrics (no `btc_poll`) |
| `sequencer.md` | Paths corrected to `via_state_keeper`; real seven-criteria `default_sealers()` plus IO-level `TimeoutSealer`; real `ViaAggregatedOperation` enum; depth pass added verbatim keeper/IO/executor/metrics structs and seal-resolution code; corrected `BTC_METRICS` (does not exist), executor paths, and config values (`block_commit_deadline_ms` 2500 is `for_tests()`, base env uses 5000; `max_pubdata_per_batch` 100000) |
| `architecture_overview.md` | Prose-level doc, largely sound; finalization stated as 2/3 threshold, bridge as n-of-n (not "threshold"), proof DA/inscription attributed to dispatcher/BTC sender rather than prover |
| `bridge.md` | Real `ViaL1Deposit` validation (system-address floor + gas coverage, not invented minimums); fabricated multi-client managers, wallet/testnet config bundles, `bridge_operations` SQL, and `via-bridge` CLI removed; current per-input withdrawal flow and `withdrawals_dal` tracking |
| `l1_indexer.md` | Real four-table schema (no `bridge_withdrawals`), verbatim `L1Indexer` loop and both processors, real `ViaIndexerConfig`/`main.rs` env loading (no `VIA_INDEXER_*` vars), real `docker/via-l1-indexer` Dockerfile and `via indexer` CLI, actual two-gauge metrics (reused `via_verifier_btc_watch` prefix) |
| `key_management.md` | Updated `create_request_headers` (merkle-root signer, `X-Sequencer-Version`), real env names (`VIA_BRIDGE_VERIFIERS_PUB_KEYS`, `VIA_BTC_SENDER_PRIVATE_KEY`), current 10-field `SystemBootstrappingInput`, merkle-root-aware taproot tweak, runtime `SystemWallets` rotation via governance; stale `recursion_scheduler_level_vk_hash` replaced with `snark_wrapper_vk_hash` flow |
| `proposer.md` | Role confirmed real (README's "Sequencer and Proposer" in `via_server`); doc mostly accurate; fixed stale `create_proof_processing_router` signature (missing `l2_chain_id`), replaced paraphrased aggregator snippets with verbatim `construct_inscription_message`, expanded enum and DA-dispatcher run loop to verbatim depth |
| `mempool.md` | Stale upstream-zksync code (MempoolStore, AccountTransactions, MempoolFetcher, `wait_for_next_tx`, MempoolGuard) replaced with verbatim `via_mempool`/`via_state_keeper` fork source (`BTreeMap` + `last_priority_id`); added `ViaPriorityOpId`, metrics, and real `MempoolConfig`; no fabricated CLIs/env vars/tables found |
| `state_management.md` | Fixed stale `ZkSyncStateKeeper` and `core/lib/state` exports, fabricated `storage_logs` SQL (real init + ALTER migrations), false Keccak claim (`hashed_key` is Blake2s-256), wrong `batch_executor/` path (real module is `executor/`); added metadata calculator and Bitcoin inscription commitment sections with verbatim source |
| `system_contracts.md` | Fixed fabricated `SystemContractUpgradeInput` (real two-phase proposal/execution structs), corrected 25 to 26 system contracts with fully verified address table (added `EvmGasManager`), replaced Ethereum-L1 claims with the Bitcoin flow (SystemBootstrapping, governance UTXO check, `forceDeployOnAddresses` upgrade tx); Solidity sources located in the pinned `vianetwork/era-contracts` submodule |
| `rpc_api_layer.md` | Heavily fabricated (12+ invented `via_` methods, fake coordinator JSON-RPC, fake TOML configs, fake `MempoolInfo` struct, `web3_sha3`); fully rewritten: real `via` namespace has exactly `via_getBitcoinNetwork` + `via_getDaBlobData`, coordinator documented as its REST/axum service with signed headers, verbatim trait/impl/router/config code and real `etc/env/base/api.toml` ports |
| `cli_reference.md` | Almost entirely fabricated (12 of 14 documented commands never existed: `via-db-setup`, `via-generate-keys`, `via-loadtest`, ...); rewritten around the real TypeScript `via` CLI (24 commands quoted from `infrastructure/via/src/`) and the three Rust binaries' actual clap surfaces; real `ViaComponents` parser; workflows re-grounded in `docs/via_guides/development.md` |
| `configuration.md` | ~20 fabricated env vars and 5 fabricated config keys removed (`VIA_CHAIN_ID`, `coordinator_pub_key`, `zk_agreement_threshold`, fee caps, ...); rewritten with 10 verbatim config structs, 12 real TOML excerpts, the real envy prefix table, `via env` compile flow, and corrected genesis/Celestia/EN values |
| `database_schemas.md` | 6 era tables had reconstructed DDL replaced with verified migration facts; 17 missing via tables added verbatim across the three databases (main, verifier, indexer); `via_bridge_tx` create-then-drop lifecycle documented; fabricated index/partitioning/backup sections removed |
| `genesis_bootstrapping.md` | ~50% fabricated (360-line `TestnetBootstrapManager` section, 6-field bootstrap struct) removed; verbatim 10-field `SystemBootstrappingInput`, `ViaBootstrap::process_bootstrap_messages`, all three storage initializers, `ViaGenesisConfig`/`VIA_GENESIS_` env flow, real testnet txids and `via bootstrap` CLI added |
| `external_node.md` | Six major fabrications fixed: reorg-detector claim inverted (Bitcoin-based `ViaNodeReorgDetectorLayer` is mandatory), "dump mode" removed, disabled `via_getBridgeAddress` no longer presented as active, fake TOML replaced with real `via_ext_node.toml`, wrong chain ids/defaults corrected, BTC stack documented as required; follower-mode btc-watch claim confirmed correct |
| `prover.md` | All five section-5 code blocks fabricated; stale legacy job-table names replaced with the real `*_fri` tables; missing RecursionTip round added (Scheduler is round 4, not 3); rewritten with verbatim gateway/witness-generator/prover/compressor mains and a sourced Via proof path (DA dispatch, `via_zk_verifier`, versioned `via_verification`) |
| `prover_gateway.md` | Mostly accurate but abbreviated; fabricated metrics section replaced with the real single `http_error` counter; "Core verifies proofs" corrected (submit_proof stores and cross-checks, does not verify the SNARK); verbatim `PeriodicApi` impls, `FriProverGatewayConfig`, core-side router, and the Via DA/verifier flow added |
| `cryptography_primitives.md` | Two factual errors fixed (mini Merkle tree bound is 2^32 leaves not 1,024; `get_tr_sighash` does not exist, real API is per-input `get_tr_sighashes`); fabricated `Sha256Hasher` usage claim corrected (zero consumers; real SHA-256 use is coordinator auth); `secp256k1_musig2` clarified as an alias of `secp256k1` 0.30; placeholder snippets replaced verbatim |
| `execution_environment.md` | Fabricated storage traits and gas model replaced; stale `VmInterface` (real: `push_transaction`/`inspect`/`finish_batch(PubdataBuilder)`), `CurrentExecutionState` field types, and `MultiVmSubversion` corrected; `VmBitcoin1_0_0` documented as dispatching to `vm_latest` (no separate Bitcoin VM); full `VmVersion` enum and execution bodies verbatim |
| `p2p_networking.md` | Core verdict confirmed: Via has no P2P layer (EN syncs over JSON-RPC, verifiers use the coordinator REST API, consensus gossip stack ships in-tree but is not wired); three stale code blocks replaced verbatim; misattributed upstream warning relocated to `core/node/consensus/src/en.rs`; auth middleware and Bitcoin RPC sections added |
| `workflow_analysis.md` | Four fabricated code blocks replaced (deposit extraction/processing, withdrawal session); OP_RETURN format corrected (`VIA_WI` + version + ids, not `VIA_PROTOCOL:WITHDRAWAL:` + proof txid); "majority" finalization corrected to the 0.66 threshold; sealers and prover job tables fixed; DA chunking, inscription construction, attestation, and reconciliation gaps filled |
| `design_patterns.md` | Full rewrite: none of the ~17 original pattern claims were code-grounded (generic CQRS/Event Sourcing/Actor Model boilerplate); replaced with 11 verified patterns anchored in verbatim source (WiringLayer DI, MessageProcessor strategy family, sealed DAL, FallbackDaClient decorator, versioned verification, fail-stop circuit breakers with no half-open state, fixed-delay retry) |
| `bottlenecks/system_bottlenecks_and_optimizations.md` | Fabricated infra-sizing analysis (GPU/RAM/PV/NFS/ROI tables) removed; rewritten around 8 code-verified bottlenecks (one-batch-per-inscription BTC sender, n-of-n MuSig2 liveness with single coordinator, WITHDRAWAL_LIMIT 7 / 660 sats, Celestia blob limit + 500 KiB chunks, watcher chunk 50 vs indexer 100, FeeRateLimits 1-100, slots 250 / pool 50, prover timeouts); suggestions quarantined and marked |
| `dependencies.md` | Eight version corrections (consensus =0.7.0, kzg/zk_evm =0.150.18, prost, rocksdb, three coexisting secp256k1 versions); `musig2` confirmed plain crates.io 0.2.x, not a fork; bitcoin/celestia-lumina/prover/gitmodules sections added with verbatim manifest excerpts; ungrounded hardware claims removed |
| `examples_and_utilities.md` | 12 invented Rust blocks removed (fictional `ZkProofVerifier`, `MuSig2Session`, `MultiClientManager`, ...); replaced with verbatim source from the 24 real examples across four crates; CLI flags corrected (`create-update-gov`, `--outDir`, `--bridge-address`); 18 undocumented examples plus CLI/bin/compose surface added |
| `loadnext_tool.md` | Rewritten around the real fork `core/tests/via_loadnext` (binary `via_loadnext`, `via run loadtest`, regtest-only); ~10 fabricated env vars and five invented tx types removed (real: Deposit/Withdraw/DeployContract/L2Execute, weights 0.05/0.5/1.0); full env/default table and flow from `executor.rs` added; upstream `core/tests/loadnext` documented as unusable Ethereum-path tooling |
| `system_upgrade_process.md` | Fabricated single-inscription upgrade model replaced with the verified two-phase flow (permissionless `SystemContractUpgradeProposal` inscription, governance `VIA_PROTOCOL:UPGRADE` OP_RETURN referencing the proposal txid); fake "activation block" scheduling removed (activation is at the first batch with a new version); processor name and wire tag corrected; 15+ verbatim symbols added end to end |
| `README.md` | Marketing prose kept; code-level claims fixed: bridge custody wording corrected to n-of-n MuSig2, disabled `via_getBridgeAddress` claim removed, EN follower-mode note aligned with source, stale "Recent Updates" section (which echoed removed fabrications) replaced with a verification-status section pointing here |

## Coverage

As of 2026-07-02, every document in this repository has been verified against via-core `e383a2990`. There are no unverified files remaining.
